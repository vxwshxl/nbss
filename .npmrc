# next/image ships an optional sharp binary; allow its postinstall so image
# optimisation works instead of silently falling back.
enable-pre-post-scripts=true

# eslint-config-next loads its plugins by bare name, which pnpm's strict
# node_modules layout hides. Hoisting the eslint packages makes them resolvable.
public-hoist-pattern[]=*eslint*
