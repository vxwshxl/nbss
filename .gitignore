# dependencies
/node_modules
.pnpm-store

# next
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# runtime data — submissions are per-deployment, never committed
/data/submissions.json
/data/*.tmp

# env
.env
.env.local
.env*.local

# editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Out-of-the-way build dir, so a production build never disturbs a running dev server.
/.next-build/
