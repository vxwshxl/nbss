{
  "name": "@nbss/shared",
  "version": "1.0.0",
  "private": true,
  "description": "What the web console and the Expo app must agree on: the database's shape, the identity rules, and the geofence maths.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./db": "./src/db.ts",
    "./identity": "./src/identity.ts",
    "./geo": "./src/geo.ts",
    "./location": "./src/location.ts",
    "./sos": "./src/sos.ts",
    "./realtime": "./src/realtime.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint config — typecheck covers this package)\"",
    "clean": "rm -rf tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
